# Session closeout — September 7, 2026 local / September 8 UTC

This is a source and recovery checkpoint, not a public release. No absolute
zero-loss guarantee is possible; verification scope and remaining limits follow.

## Resume exactly here

Say **“Resume AI Chief of Staff from the latest session closeout.”**
Use `/Users/brettlechtenberg/dev/ai-chief-of-staff`, branch
`checkpoint/2026-09-06-session-closeout`, not an older `main` or Desktop copy.
Read `AGENTS.md`, `RESUME-PROMPT.md`, current `RECOVERY.md`, `CLAUDE.md`, and
`CONTEXT.md`. Desktop pointer:
`~/Desktop/Resume Prompts/AI-CHIEF-OF-STAFF-RESUME.md`.

Both Hook Lab upgrades are installed in the signed/notarized private Intel app:
scene timing checks and exact full-script save/load/Video Studio handoff.
The private version remains `1.0.0-beta.25`; no release tags or public updates.
Do not rebuild completed work. Next development is still connector validation,
not repeating the consumed finance, SEO or Hook Lab generations.

## Verified closing state

- **RUNTIME:** native inspection found eight visible editable controls and no
  nonempty non-default values. No values were exported. Normal macOS Quit
  succeeded; an exact process census confirmed the installed app was stopped.
- **RUNTIME:** `npm run typecheck`, `npm run lint`, and `npm test` passed:
  **101 files / 1,824 tests**. Native/browser acceptance from installation remains
  applicable because no product code changed during closeout.
- **RUNTIME:** fresh stopped-app checkpoint:
  `~/Library/Application Support/acos-local-improvement-backups/checkpoint-step18-HQEWjy`.
  Snapshot began `2026-09-08T00:21:36.922Z`.
- Main SQLite: 19 tables / 540 rows; restore-and-verify **22 ms**. Finance:
  restore-and-verify **14 ms**. Persistent files: ten roots / 40 entries,
  copy/restore/verify **96 ms**. Prior installed-bundle copy/verify **24,340 ms**.
- Bundle manifest: `bbf75251e31d910f63d0d859d6b806b5f260734fa1e6daca97cb9af13f909d7c`.
  `verification.json` and `restore-drill/` are in the checkpoint. No live restore
  was applied. Timings measure local drills, not total disaster-recovery time.
- **RUNTIME:** after the full suite, read-only comparison checked 32 app/finance
  tables: every original row, including settings, remained identical. Google
  credentials/tokens and MCP configuration files were unchanged.

The app must stay stopped for archival. On resume, use the existing validated
private-bundle launch with `ACOS_INSTALL_VALIDATION=1`, not a normal Dock launch.
Keep automatic services paused and preserve Google access and consumed markers.

## GitHub and Vercel

GitHub is **public**: `BrettLechtenbrerg/ai-chief-of-staff`. Only reviewed source,
tests and handoff documents may be pushed to the existing checkpoint branch.
Keep `.gg/`, credentials, app data, private bundles and generated reports out.
Source upload and secret-scan receipts are recorded below once verified.

**RUNTIME:** Vercel inspection of `https://www.totalsuccessai.com` returned project
`tsai-site`, deployment `dpl_CjNkrHixroYLHJGVXscvvBUYQ8Fb`, state **READY**.
The desktop app is not a Vercel deployment; its separate landing page had no
changes from this session requiring deployment. No Vercel deployment, public app
release or unrelated website modification was performed.

## Private archival and limits

The current SQLite/persistent-file checkpoint is verified locally. A broader
private archive is being prepared separately under `~/dev/_backups/`, including
repository history, ignored `.gg/` helpers/approvals, dependencies, built bundles,
full stopped app data, shared Flo data, brand profiles and resume routing.

Private copies on the external drive or iCloud must be **encrypted first**.
The user enters the encryption password privately in Terminal, never in chat.
Do not overwrite or rerun the older completed September 6 backup recipe.
Do not call the new external backup complete until its completion receipt and
read-only external restore verification exist. iCloud-local checksum equality
alone does not prove server upload. Local-only backups do not survive disk loss.

No Keychain export is permitted. A new Mac may require normal reauthentication.
Old app versions cannot read newly saved v2 script records: preserve the newer
library and use a compatible reader during recovery. These backups are snapshots,
not continuous protection. Preserve the existing installed-app rollback bundles.

## Final receipts

Pending completion of the current source upload and private archival steps.
